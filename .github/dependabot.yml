version: 2
updates:
    -
        package-ecosystem: composer
        directory: "/"
        schedule:
            interval: monthly
        versioning-strategy: auto
        groups:
            dev-dependencies:
                dependency-type: "development"
